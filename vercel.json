{
    "version": 2,
    
    "functions": {
        "api/install.sh": { "runtime": "vercel-dev-runtime@0.0.4" }
    },
      
    "builds": [
        {
            "src": "./server.py",
            "use": "@vercel/python"
        },
        {
            "src":"./build/**",
            "use":"@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.py"
        }
    ]
}