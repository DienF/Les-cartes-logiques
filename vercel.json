{
    "version": 2,  
    "installCommand":"npm install;npm run build;pip install -r requirements.txt",
    "functions":{
        "server.py":{ 
            "includeFiles":"../build/**"
        }
    },
    "routes": [
        {
          "src": "/(.*)",
          "dest": "api/server.py"
        }
      ]
}