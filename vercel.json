{
    "version": 2,
    "buildCommand":"npm run build",
    "installCommand":"npm install",
    "builds": [
        {
            "src": "./server.py",
            "use": "@vercel/python"
        },
        {
            "src": "./react/build/**",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./server.py"
        }
    ]
}