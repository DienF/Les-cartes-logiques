{
    "version": 2,  
    "installCommand":"npm install;npm run build;pip install -r requirements.txt",
    "functions":{
        "server.py":{ 
            "includeFiles":"./build/**"
        }
    },
    "rewrites": [
        { "source": "/(.*)", "destination": "/server" }
    ]
}