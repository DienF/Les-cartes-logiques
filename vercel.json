{
    "version": 2,
    "functions":{
        "api/install.sh":{ 
            "runtime":"vercel-bash" 
        }
    },
    "builds": [
        {
            "src": "./server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.py"
        }
    ]
}