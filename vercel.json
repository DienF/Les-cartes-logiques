{
    "version": 2,  
    "installCommand":"npm install;npm run build;pip install -r ./api/requirements.txt",
    "functions":{
        "server.py":{ 
        }
    },
    "rewrites": [
        { "source": "/(.*)", "destination": "/api/server" }
    ]
}